services:
  postgres:
    image: postgres:16
    container_name: vanopt-postgres
    environment:
      POSTGRES_DB: vanopt
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"